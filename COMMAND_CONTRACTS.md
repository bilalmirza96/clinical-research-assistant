# Command Contracts

This file moved to **`clinical-research-assistant/skills/references/command-contracts.md`** so it sits alongside the other machine-consumable references.

Skills read it via the relative path `../../references/command-contracts.md` from inside `skills/internal/<skill>/SKILL.md`.

> Why moved (v3.3): co-locating contracts with skills' own references makes each skill's PREREQUISITE block able to verify its own contract without crossing the plugin-root boundary.
