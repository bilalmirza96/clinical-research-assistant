# State Schema

This file moved to **`clinical-research-assistant/skills/references/state-schema.md`** so it sits alongside the other machine-consumable references (`writing-style.md`, `kdense-delegations.md`, `biomedagent-methodology.md`).

Skills read it via the relative path `../../references/state-schema.md` from inside `skills/internal/<skill>/SKILL.md`.

> Why moved (v3.3): keeping the state schema next to the other shared references lets each skill's PREREQUISITE block read its contract directly without crossing the plugin-root boundary.
